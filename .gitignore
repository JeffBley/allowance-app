# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Azure Functions local settings (contains secrets/connection strings)
api/local.settings.json
api/dist/

# AZD environment state (contains provisioned resource names and env outputs)
# The .azure/ folder is intentionally kept for plan.md but .env files within it are not
.azure/**/.env
.azure/**/config.json

# Local env overrides (contains VITE_ secrets for local dev)
.env.local

# Generated at build time by azure.yaml prepackage hook — contains VITE_ secrets
.env

# Internal security tracker — MUST NOT be committed
.internal/
