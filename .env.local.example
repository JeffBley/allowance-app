# Local development environment variables
# Copy this file to .env.local — it is gitignored and should never be committed.
#
# After running `azd provision`, these values are available via:
#   azd env get-values

# From Entra External ID app registration (bleytech.onmicrosoft.com)
VITE_CLIENT_ID=__REPLACE_WITH_APP_REGISTRATION_CLIENT_ID__
VITE_TENANT_ID=0b59b81c-986d-4a76-9777-cc5a883950b5
VITE_AUTHORITY=https://bleytech.ciamlogin.com/

# From `azd provision` output — AZURE_FUNCTION_APP_NAME.azurewebsites.net
VITE_API_URL=http://localhost:7071/api
